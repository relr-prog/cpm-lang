// integer arithmetic + precedence + unary minus
fn main() -> i32 {
    var x: i32 = 2 + 3 * 4;
    println_i32(x);
    var y: i32 = (2 + 3) * 4;
    println_i32(y);
    println_i32(x / 2);
    println_i32(x % 5);
    println_i32(-x);
    return 0;
}