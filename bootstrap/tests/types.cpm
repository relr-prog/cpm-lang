fn add(a: i32, b: i32) -> i32 {
    return a + b;
}

fn main() -> i32 {
    var n: i32 = add(20, 22);
    var ok: bool = (n == 42) && true;
    var f: f64 = 1.5 + 2.5;
    var s: str = "cpm";
    println_i32(n);
    println_bool(ok);
    println_f64(f);
    println(s);
    return 0;
}
